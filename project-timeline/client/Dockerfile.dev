# client/Dockerfile.dev
# Used by docker-compose.dev.yml — runs Vite dev server with HMR
FROM node:20-alpine
WORKDIR /app
COPY package.json package-lock.json* ./
RUN npm install
COPY . .
EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
